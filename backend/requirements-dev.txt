# These dependencies are kept separate from requirements.txt so the production Docker image doesn't ship pytest and its transitive dependencies
-r requirements.txt
pytest==8.2.0
httpx==0.28.1
ruff==0.15.14